apiVersion: v1
kind: Service
metadata:
  name: sample-webserver
  labels:
    app: sample-webserver
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: sample-webserver
